{$RELAY_DOMAIN} {
    handle /ws {
        reverse_proxy relay:8765 {
            header_up X-Real-IP {remote_host}
            flush_interval -1
        }
    }
    handle /health {
        respond "RemoteInput relay" 200
    }
    handle {
        respond 404
    }
}
